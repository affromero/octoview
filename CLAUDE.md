# octoview — project instructions

CLAUDE.md is the ONLY agent-instructions file in this repo — do not add
AGENTS.md or variants; point other tools here.

octoview is a Safari/Chrome/Firefox MV3 browser extension that previews ML and
data files inline on GitHub blob pages. The content script runs on `github.com`
with the user's session cookies, fetches the raw file bytes, and renders them in
a pane that replaces the code view. It reaches private repos because it rides
your existing GitHub session — no service, no upload, no PAT.

## Layout & data flow

- `extension/` is the **Safari source of truth**. Chrome and Firefox variants
  are generated by `scripts/build-chrome.mjs` / `scripts/build-firefox.mjs`
  (never edit `build/`, it is gitignored).
- `content.js` — the github.com content script: injects the Preview button,
  fetches the raw blob, dispatches by extension, builds the pane. Uses
  `browser.*` (aliased to `chrome` for Chrome).
- `core.js` — pure + DOM logic (dispatch, raw-URL resolution, notebook render,
  markdown sanitize). **Stays free of `browser.*`** so vitest covers it.
- `render-*.js` — lazy-imported renderers (`render3d`, `render-splat`,
  `render-array`, `render-table`, `render-model`, `render-onnx`, `render-image`).
  `render-splat` draws TRUE anisotropic gaussians (EWA splatting) in pure WebGL2
  on the main thread — no worker/WASM/eval/blob — so it runs under the strict CSP
  in every browser. It builds each splat's covariance in a float data texture and
  sorts back-to-front every frame (16-bit counting sort).
- `splat-decode.js` — **pure** splat decoders (no DOM, no `browser.*`), so
  vitest covers them. Renderer contract: `{ count, pos:Float32[3n],
col:Float32[4n rgba 0..1], scale:Float32[3n] (linear std dev), quat:Float32[4n]
(xyzw, normalized) }`, subsampled to `MAX_SPLATS`.
- `viewer.html` — the extension page whose CSP octoview controls; hosts the
  live-report sandbox. (There is no Spark/splat viewer page anymore — splats
  render inline in the content script's isolated world, like the other 3D.)
- `vendor/*` — self-contained libs, all rebuilt by `scripts/vendor.sh` from
  lockfile-pinned npm deps.

## Security is the product — standards

This extension renders untrusted files (any file from any repo) while holding
the user's GitHub session. Treat every file, and any repo README that links to
one, as hostile. `SECURITY.md` is the public statement; these are the rules.

### Trust boundary / sandbox invariant (never weaken)

- Untrusted HTML (reports, notebook `text/html` outputs) runs ONLY inside a
  `sandbox="allow-scripts"` iframe — **never add `allow-same-origin`**. That
  opaque origin cannot read github.com cookies/DOM, call extension APIs, or make
  requests as the user. `connect-src 'none'` additionally denies it network.
- Untrusted HTML must never reach `innerHTML`/`srcdoc` outside that sandbox.
  File-derived strings (tensor/ONNX/column/manifest names, PLY comments) go to
  the DOM via `textContent` or `setAttribute` only.
- postMessage: validate `e.origin === 'https://github.com'` AND/OR
  `e.source === <the frame we created>` on receive. The report HTML is posted
  with targetOrigin `'*'` on purpose (Chrome sandbox pages have an opaque
  origin) — safe only because it goes to our own just-created frame and carries
  the already-open file, not a secret. Never broadcast bytes or secrets with `*`.
- Fetch is `credentials:'same-origin'` against github.com only; cookies never
  cross to `raw.githubusercontent.com` on redirect. **No** telemetry, beacon,
  WebSocket, or any request to a non-GitHub host — ever.

### Parsers must be defensive (untrusted binary → main thread)

Every decoder in `splat-decode.js` / `render-model.js` / `render-onnx.js` /
`render-array.js` handles hostile input. A crafted file must **fail fast**, never
hang or OOM the tab. Rules:

- **Validate header-declared sizes against the actual buffer before allocating.**
  A count/length field (spz `total`, gguf array `n`, npy shape, TOC sizes) is
  attacker-controlled: cap it and check it against `buffer.length` before
  `new Float32Array(count*…)` or a loop bound.
- **Cap decompression.** gzip/zstd/zip expand ~1000×. `unzip.js` enforces a
  64 MiB CAP (per-entry AND total); spz gzip uses `gunzipCapped` (streaming, 256
  MiB); spz v4 validates `total` before sizing the zstd output buffer. Never
  call `gunzipSync`/`decompress` unbounded on file bytes.
- **Bound every file-driven loop.** A protobuf varint caps at 10 bytes + EOF
  check; a pointer-advance loop (gguf `skip`) bounds-checks the offset after
  every step (a fixed-width skip does no DataView read, so it won't throw on its
  own); graph node counts cap at 20k.
- Prefer `DataView` reads (they throw RangeError on OOB = safe) over raw typed-
  array indexing (returns `undefined`→NaN silently). Where raw indexing is
  unavoidable, validate the extent first (e.g. `.sog` requires each image ≥
  `count*4` texels; the webp readback caps at 64M pixels).

### Markdown sanitizer (`core.js` `sanitize`)

marked output is stripped before it hits the DOM: remove script-ish elements and
`on*` handlers; on URL-bearing attributes strip `\x00-\x20` (ALL C0 controls +
space, because browsers trim those before parsing a scheme) then block
`javascript:` / `data:` / `vbscript:`. Stripping only `\s` was a real bypass
(`\x01javascript:`). This is defense-in-depth behind GitHub's CSP — keep it
working on its own.

### Supply chain / vendoring

- Every `vendor/*` lib is bundled by `scripts/vendor.sh` via esbuild from a
  **lockfile-pinned npm devDependency**. **Never curl from a floating CDN URL**
  (that was a real finding for `marked`). Pin the version; commit the
  lockfile entry.
- Gitleaks (secret scan), `npm audit --audit-level=high` (push/PR + weekly), and
  Dependabot (weekly, 7-day cooldown) run in CI. `npm run audit` is the local
  gate. Keep them green.

### Per-browser CSP (never merge the manifests)

The only CSP variation left is the live-report path; splats no longer need any
relaxation (the native WebGL2 renderer runs in the content script's isolated
world, bound by no extension-page CSP).

- Safari `extension_pages` keeps `'unsafe-inline'` (the report viewer's inline
  capability probe); it also carries `connect-src 'none'` and `object-src 'none'`.
- Chrome/Firefox reject `'unsafe-inline'` outright (whole extension fails to
  load), so the build scripts rewrite to the strict minimum (`script-src 'self';
connect-src 'none'; object-src 'none'`). Chrome additionally makes `viewer.html`
  a `sandbox` page with its own `content_security_policy.sandbox` (inline allowed
  there); Firefox has no sandbox key, so live reports fall back to static.

### Accepted tradeoffs (deliberately NOT changed — don't "fix" these)

- **Content script matches all `github.com/*`, not just `/blob/`.** Narrowing
  would break the common flow: GitHub is an SPA, so the script must already be
  present on a repo page to re-sync when the user navigates to a file. It
  no-ops off blob pages.
- **`use_dynamic_url` is off.** It blunts install fingerprinting but breaks the
  content script's `import(getURL(...))` of the render modules (verified). A Low
  mitigation is not worth breaking module loading.

## Formats & decoders

- Renderer contract everywhere: `{ count, pos, col, scale, quat }`. The renderer
  draws TRUE anisotropic gaussians (oriented covariance ellipses) from the scale +
  quaternion. Spherical harmonics are still dropped (base color only). `.splat`
  and both `.ply` variants are fully anisotropic; `.spz`/`.ksplat`/`.sog`/`.lcc`
  currently emit identity rotation (isotropic look) pending a per-format rotation
  cross-check — decode their rotation streams and drop the `ponytail:` note when
  verified against the sample render.
- Splats: `.splat`, 3DGS + compressed `.ply`, `.spz` (v1-4, hand-written from
  nianticlabs/spz — Spark's own reader can't open v4), `.ksplat` (mkkellogg;
  scales are LINEAR, no exp), `.sog` v2 (zip of webp textures, codebook
  scales/colors, inverse-log means; webp decoded via WebGL2 readback with
  premultiply off — a 2D canvas corrupts the alpha-carrying data channels).
  `.splattie` (rigged Spark widget) and `.rad` (Spark-only LOD format) are NOT
  supported — they needed Spark, which was removed (see "Why not Spark" below).
- `.lcc` (XGRIDS) is a MULTI-FILE container: the `.lcc` metadata blob plus sibling
  `index.bin` + `data.bin` fetched from the same GitHub directory (hence the
  `media.githubusercontent.com` host permission). `parseLcc` (in
  `splat-decode.js`) sums the LOD
  records — so it must guard against overlapping records inflating the count past
  `data.bin/32` and cap to `MAX_SPLATS` (it had neither; both added).
- Tables: `.parquet` (hyparquet), `.arrow`/`.feather`/`.ipc` (flechette — chosen
  over apache-arrow, which is 8× larger and defeats tree-shaking).
- Model graphs: `.safetensors`/`.gguf` (header tables), `.onnx` (hand-rolled
  protobuf wire reader + a longest-path SVG graph; NOT protobufjs, too heavy).
- Fixtures are generated from real external encoders, not our own decoders (same-
  scene cross-checks catch format misreads): `@playcanvas/splat-transform` (sog,
  spz, spz v4), mkkellogg `create-ksplat`, pyarrow (arrow), `onnx.helper` (onnx).
  Commit the tiny result under `samples/`.

### Why not Spark (why we render splats ourselves)

Spark is a full Gaussian-splat renderer, but it's a ~5.7MB WASM+worker library,
and an MV3 extension can't give it what it needs. It creates its sort worker from
a `blob:` URL and fetches its WASM from a `data:` URL; extension-page CSPs block
both (`connect-src 'none'`, no blob workers — Firefox WONTFIX'd blob workers with
no exemption). So Spark silently failed to initialize in EVERY browser and splats
fell back to an isotropic approximation. Rendering the gaussians ourselves — a
~40-line EWA-splatting WebGL2 shader on the main thread — needs none of that: it
runs under the strict CSP everywhere, drops a 5.7MB dependency, and works in
Firefox too. The cost is spherical-harmonics color and the `.rad`/`.splattie`
Spark-only formats. **Do not re-add Spark** to "improve" splat rendering; extend
`render-splat.js` / `splat-decode.js` instead.

## Commands (the gates)

- `npm run ci` — lint + format check + vitest (the commit gate).
- `npm run test:e2e` — every sample rendered in Chromium AND WebKit (the
  Safari-truth harness; a format is only "supported" when it passes here).
- `npm run test:chrome` — the built Chrome extension loaded into real Chromium
  (button injection, live report in the sandbox page, native splat render).
- `npm run test:firefox` — Firefox build + `addons-linter` (AMO's validator;
  `web-ext` crashes natively on this machine — use addons-linter).
- `npm run audit` — `npm audit --audit-level=high`.
- `npm run screenshots` — regenerate store screenshots (when the pane UI changes).
- `./scripts/build-safari.sh` — signed Mac App Store archive (team 2HVQQ4W769).

## README must follow the code

The README is the product page AND the architecture doc. Update it in the SAME
change that alters what it describes:

- **Architecture Mermaid** (`## How it works → ### Architecture`): new/renamed/
  removed extension files, viewer pages, message flows, build variants, or
  per-browser CSP behavior.
- **Format table** (`## What it previews`): any renderer/decoder added, promoted,
  or removed — ✅ ONLY with a passing WebKit e2e case.
- **Browser table + footnote** (`## Related work`): Safari/Chrome/Firefox
  capability differences.
- **Screenshots** (`assets/screenshots/`): regenerate via `npm run screenshots`
  when the pane UI visibly changes.
